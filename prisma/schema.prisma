generator client {
  provider = "prisma-client-js"
  output   = "client"
}

datasource db {
  provider = "sqlite"
  url      = "file:../database.sqlite"
}

model memory {
  id   Int     @id @default(autoincrement())
  date String?
}

model signed_chats {
  chat_id String @id
}

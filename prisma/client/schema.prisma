generator client {
  provider = "prisma-client-js"
  output   = "client"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model memory {
  id   Int     @id @default(autoincrement())
  date String?
}

model signed_chats {
  chat_id String @id
}
